apiVersion: maistra.io/v1
kind: ServiceMeshMemberRoll
metadata:
  name: default
  namespace: {{ .Namespace }}
spec:
  members:
    - bookinfo
    - foo
    - bar
    - legacy
{{- range $i := until .Count }}
    - test{{ $i }}
{{- end }}