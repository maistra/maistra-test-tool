apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: httpbin-ext
spec:
  hosts:
    - {{ .Name }}.{{ .Namespace }}
  ports:
    - number: 8000
      name: http
      protocol: HTTP
  resolution: DNS
  location: MESH_EXTERNAL
