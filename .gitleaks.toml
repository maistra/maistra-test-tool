# Global allowlist
[allowlist]
  description = "Testdata sample cert files"
  files = [
      "^sampleCerts/(.*?)$",
      "^testdata/examples/(.*?)$",
      "^scripts/federation/(.*?)$",
      ".gitleaks.toml",
  ]
